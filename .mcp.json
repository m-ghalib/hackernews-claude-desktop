{
  "mcpServers": {
    "hackernews": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/dist/index.cjs"
      ],
      "env": {
        "HN_DEFAULT_USERNAME": "${user_config.username}"
      }
    }
  }
}
